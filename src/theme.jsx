import {createTheme} from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#8CB369',
      light: '#F4E285',
    },
    secondary: {
      main: '#f50057',
    },
  },

});
