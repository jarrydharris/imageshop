import {createTheme} from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#aed581',
      light: '#f44336',
    },
    secondary: {
      main: '#f50057',
    },
  },
});